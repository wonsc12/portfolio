String 객체(문자열 다루기)

메소드(함수)

문자열.indexOf()
문자열.slice()
문자열.split()
문자열.subString()
문자열.subStr()
문자열.concat() 분리된걸 합체
문자열.replace()
문자열.replaceAll()
문자열.toUpperCase()
문자열.toLowerCase()
문자열.trim()

프로퍼티(변수)
문자열.length

Math 객체
Date 객체
DOM 객체

-텍스 애니메이션 (한글자 등장 및 사라지기)
-kwon do hyeon ->대문자로 치환 KWON DO HYEON

-Kwon dO HYeon -> 소문자로 치환

-구인구직 플랫폼 자기소개
특정단어 찾아서 그단어랑 매칭이 되면 입력이 안되도록

-입력한 단어->다른 단어로 교체될 때
-특정단어가 몇번째서부터 시작되는지 시작순번값 알려주셈
-

2시

5줄 text 반복문

둘다 입력 x 경고창
형식에 맞게 재대로입력됬다는 메시지

key up 이벤트
버튼클릭 이벤트

이메일과 
패스워드는 라벨태그임
스판태그로 하나씩 분리

